generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  id       Int    @id @default(autoincrement())
  name     String
  email    String @unique
  password String

  createdAt DateTime @default(now())
  updatedAt DateTime? @updatedAt

  task Task[]
}

enum TaskStatus{
  PENDING
  COMPLETED
  MISSED
}

enum TaskType{
  DAILY
  ASSIGNED
  SPECIFIC
}

model Task{
  id Int @id @default(autoincrement())
  user User @relation(fields: [userId], references: [id])
  userId Int

  title String
  description String?
  deadline DateTime
  status TaskStatus @default(PENDING)
  type TaskType @default(SPECIFIC)

  createdAt DateTime @default(now())
  updatedAt DateTime? @updatedAt
}